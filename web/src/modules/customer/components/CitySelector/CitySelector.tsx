import React from 'react';
import { bem } from '@utils/formatters';
import './CitySelector.scss';

const cn = bem('CitySelector');

interface Props {

};

const CitySelector: React.SFC<Props> = (props) => {
    const {

    } = props;

    return (
        <div className="CitySelector"></div>
    );
};

export { CitySelector };
